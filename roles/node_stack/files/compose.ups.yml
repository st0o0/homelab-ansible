services:
  nut-upsd:
    image: instantlinux/nut-upsd
    container_name: node-stack-nut-upsd
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=${TZ}
      - DRIVER=${NUT_DRIVER}
      - NAME=${NUT_NAME}
      - PORT=${NUT_PORT}
      - POLLINTERVAL=${NUT_POLLINTERVAL}
      - API_USER=${NUT_API_USER}
      - API_PASSWORD=${NUT_API_PASSWORD}
    devices:
      - /dev/bus/usb:/dev/bus/usb
    ports:
      - "3493:3493"
    volumes:
      - ./nut:/etc/nut

  peanut:
    image: brandawg93/peanut:latest
    container_name: node-stack-peanut
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
    volumes:
      - ./peanut:/config
    ports:
      - "8080:8080"
    environment:
      - PUID=1000
      - PGID=1000
      - WEB_PORT=8080
