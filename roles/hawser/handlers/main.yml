- name: Restart node-stack
  community.docker.docker_compose_v2:
    project_src: "{{ node_stack_deploy_path }}"
    state: restarted
    remove_orphans: true
  ignore_errors: "{{ ansible_check_mode }}"
