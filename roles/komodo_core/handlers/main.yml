- name: Restart komodo_core
  community.docker.docker_compose_v2:
    project_src: "{{ komodo_core_deploy_path }}"
    state: restarted
    remove_orphans: true
  ignore_errors: "{{ ansible_check_mode }}"
